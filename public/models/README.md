Place Rhino-exported GLB files here.

Expected default model:
libero.glb

Default public URL:
/models/libero.glb

Additional available model:
selene.glb

Required material names for configurator:
mat_bath_outer
mat_bath_inner
mat_hardware

For production with many or large GLB files, consider CDN/object storage with proper CORS headers.
