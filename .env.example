# Future email/PDF backend settings.
# Do not expose secrets in frontend code.

EMAIL_PROVIDER=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Optional public configuration.
VITE_DEFAULT_MODEL_ID=libero
VITE_DEFAULT_MODEL_NAME=Libero
VITE_DEFAULT_GLB_URL=/models/libero.glb
